<div class="container">
  <div class="row">
    <div class="col-md-8">
      <h1>商品新規登録</h1>
      <!-- %= render 'form', product: @product %-->

      <div>
        <table>
        <%= form_with model: @product, url: admin_products_path, local:true do |f| %>
          <tr>
            <td>商品画像</td>
            <td><%= f.file_field :image, accept: "image/*" %></td>
          </tr>
          <tr>
            <td>商品名</td>
            <td><%= f.text_field :name %></td>
          </tr>
          <tr>
            <td>商品説明</td>
            <td><%= f.text_area :description %></td>
          </tr>
          <tr>
            <td>ジャンル</td>
            <td><%= f.collection_select :product_genre_id, Genre.all, :id, :genre, include_blank: "選択してください" %></td>
          </tr>
          <tr>
            <td>税抜き価格</td>
            <td><%= f.text_field :price %>円</td>
          </tr>
          <tr>
            <td>販売ステータス</td>
            <td>
              <%= f.radio_button :is_active, 'true' %>販売中
              <%= f.radio_button :is_active, 'false' %>販売停止中
            </td>
          </tr>
          <tr>
            <td></td>
            <td><%= f.submit '新規投稿' %></td>
          </tr>
        <% end %>
        </table>
      </div>

    </div>
  </div>
</div>